PORT=3000
AUTH_SERVICE_HOST=auth # service name in docker compose file
# AUTH_SERVICE_HOST=0.0.0.0 # while running locally
AUTH_SERVICE_PORT=3001

USER_SERVICE_HOST=user # service name in docker compose file
# USER_SERVICE_HOST=0.0.0.0 # while running locally
USER_SERVICE_PORT=3002